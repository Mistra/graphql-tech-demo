type Book {
    id: ID!
    title: String!
    description: String!
    author: Author!
}

type Author {
    id: ID!
    name: String!
}

type Query {
    getBook(id: ID!): Book
    getAuthor(id: ID!): Author
}